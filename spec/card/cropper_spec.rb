# frozen_string_literal: true

RSpec.describe Card::Cropper do
  it "has a version number" do
    expect(Card::Cropper::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
